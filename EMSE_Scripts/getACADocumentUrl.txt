//String
var url = "http://qa-server10.achievo.com/720/COSD";
var documentID = "10000087";
//DocumentModel
var documentModel = "";
var result;

documentModelResult = aa.document.getDocumentByPK( documentID );
if(documentModelResult.getSuccess())
{
	documentModel = documentModelResult.getOutput();
	result = aa.document.getACADocumentUrl(url,documentModel);
	if (result.getSuccess())
	{
	    aa.print("Success");
	}
	else
	{
	    aa.print("Failed:" + result.getErrorMessage());
	}
}
else
{
	aa.print("Failed:" + documentModelResult.getErrorMessage());
}
