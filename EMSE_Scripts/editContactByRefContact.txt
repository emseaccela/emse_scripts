var peopleSequenceNumber = '1000';
var result;

var contactModelResult = aa.people.getPeople( peopleSequenceNumber );
if(contactModelResult.getSuccess())
{
	contactModel = contactModelResult.getOutput();
	//edit
	contactModel.setComment("Edit by Auto Test.");
	result = aa.people.editContactByRefContact(contactModel);
	if (result.getSuccess())
	{
	    aa.print("Success");
	}
	else
	{
	    aa.print("Failed:" + result.getErrorMessage());
	}
}
else
{
	aa.print("Failed:" + contactModelResult.getErrorMessage());
}

